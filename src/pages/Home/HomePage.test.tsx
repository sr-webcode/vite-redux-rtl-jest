import { render, screen } from "@testing-library/react";
import Home from "./HomePage";

describe("App", () => {
  it("should work as expected", () => {
    // render(<App />);
    expect(1 + 1).toBe(2);
  });
});